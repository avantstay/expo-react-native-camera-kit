{
  "name": "expo-react-native-camera-kit",
  "version": "0.0.2",
  "main": "build/main.js",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --platform=node --target=node16 --packages=external --outfile=build/main.js",
    "prepublish": "npm run build"

  },
  "dependencies": {
    "@expo/config-plugins": "^7.2.5",
    "@expo/config-types": "^49.0.0-alpha.1"
  },
  "devDependencies": {
    "typescript": "^5.0.2",
    "esbuild": "^0.19.3"

  }
}
